from django.urls import path

from FruitipediaApp.fruit import views

urlpatterns = [
    # path('create/', views.index, name='index'),
    # path('<id:fruitId>/details/', views.index, name='index'),
    # path('<id:fruitId>/edit/', views.index, name='index'),
    # path('<id:fruitId>/delete/', views.index, name='index'),
]